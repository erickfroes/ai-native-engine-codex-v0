# Sprite Animation v1

Componente declarativo opt-in: `visual.sprite.animation`.

Sem graph, sem timeline, sem skeletal animation.

Use `inspect-sprite-animation` (CLI) ou `inspect_sprite_animation` (MCP) para gerar `SpriteAnimationReport v1` deterministico.
